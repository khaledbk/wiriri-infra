# Never commit secrets or live cert/state material
*.env
.env
!*.env.example
backend/.env
imgproxy.env
certbot/conf/
certbot/www/
**/minio/data/
**/cache/
*.pem
*.key

# GeoIP DB (regenerated from DB-IP Lite at build/refresh; not source)
*.mmdb
web-01/nginx/geoip/

# On-box config backups / live snapshots (state, not source)
backups/
